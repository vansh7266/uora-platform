admin:
  address:
    socket_address:
      address: 0.0.0.0
      port_value: 9901

static_resources:
  listeners:
    - name: uora_listener
      address:
        socket_address:
          address: 0.0.0.0
          port_value: 10000
      filter_chains:
        - filters:
            - name: envoy.filters.network.http_connection_manager
              typed_config:
                "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                stat_prefix: uora_ingress
                # AUTO supports both HTTP/1.1 and HTTP/2 (h2c)
                codec_type: AUTO
                use_remote_address: true

                access_log:
                  - name: envoy.access_loggers.stdout
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.access_loggers.stream.v3.StdoutAccessLog
                      log_format:
                        text_format: "%START_TIME% %REQ(:METHOD)% %REQ(X-ENVOY-ORIGINAL-PATH?:PATH)% %RESPONSE_CODE% %DURATION% %BYTES_RECEIVED% %BYTES_SENT% %REQ(X-REQUEST-ID)%\n"

                http_filters:
                  # ── Lua filter: inject nanosecond timing headers ──────────────
                  - name: envoy.filters.http.lua
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.lua.v3.Lua
                      inline_code: |
                        function envoy_on_request(request_handle)
                          -- os.time() is seconds; multiply to get nanosecond-scale epoch.
                          -- NOTE: os.time() has 1s resolution in LuaJIT. For sub-second
                          -- precision we use the Envoy start_time from access log (%START_TIME(%s%9f)%)
                          -- which is the authoritative nanosecond timestamp.
                          -- This header gives downstream services a best-effort ns marker.
                          local start_ns = os.time() * 1000000000
                          request_handle:headers():add("x-uora-start-time", tostring(start_ns))

                          -- Store in stream dynamic metadata so response phase can read it
                          request_handle:streamInfo():dynamicMetadata():set(
                            "uora.lua", "start_time_ns", start_ns
                          )
                        end

                        function envoy_on_response(response_handle)
                          local meta = response_handle:streamInfo():dynamicMetadata():get("uora.lua")
                          if meta and meta["start_time_ns"] then
                            local end_ns = os.time() * 1000000000
                            local latency_ns = end_ns - meta["start_time_ns"]
                            response_handle:headers():add("x-uora-latency-ns", tostring(latency_ns))
                          end
                        end

                  - name: envoy.filters.http.router
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router

                route_config:
                  name: uora_routes
                  virtual_hosts:
                    - name: contestant_backend
                      domains: ["*"]
                      routes:
                        # WebSocket upgrade route — must come before the catch-all
                        - match:
                            prefix: "/api/v1/stream"
                          route:
                            cluster: contestant_backend
                            timeout: 0s          # 0 = no timeout for WS
                            upgrade_configs:
                              - upgrade_type: websocket

                        # All other routes
                        - match:
                            prefix: "/"
                          route:
                            cluster: contestant_backend
                            timeout: 30s

  clusters:
    - name: contestant_backend
      connect_timeout: 5s
      type: LOGICAL_DNS
      dns_lookup_family: V4_ONLY
      # Enable HTTP/2 to upstream if contestant supports h2c
      http2_protocol_options: {}
      load_assignment:
        cluster_name: contestant_backend
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      # host.docker.internal resolves to the Docker host
                      # so Envoy (inside Docker) can reach contestant on port 8080
                      address: host.docker.internal
                      port_value: 8080